httpx>=0.27

# Makes Python verify TLS against the OS certificate store instead of
# certifi's bundle. Without it, boards behind a TLS-inspecting proxy fail with
# CERTIFICATE_VERIFY_FAILED, which is indistinguishable from a dead board.
# Optional: jobscan/__init__.py skips injection if it is missing.
truststore>=0.9

# tier B only, install when you get there:
# playwright>=1.44
